import 'package:flutter/material.dart';

class archive_Page extends StatelessWidget {
  const archive_Page({super.key, required String userId});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
